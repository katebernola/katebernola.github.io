# Unknowns

Things we know we don't know. Different from questions — these are gaps, not puzzles.

- Real character budgets for the domain components (task, intelligent dashboard) — the numbers in `../components/` for those are unmeasured.
- The actual product's approved names — `../terminology/product-language.md` is empty until they're filled in from a real source.
- Localization targets: which languages this product actually ships in. (Expansion factor now sourced: up to +50% average, +300% for short labels, per Polaris internationalization — but the real language list is still unknown.)
- Accessibility requirements beyond WCAG basics that the org may have committed to.
- How stakeholders will actually respond to strategy-first reviews — the process assumes goodwill we haven't tested at scale.
